Project/runtime instructions are already loaded by the Codex harness; do not reread AGENTS.md or CLAUDE.md. If docs/00-ai-context.md exists, use it as the project entry point; otherwise use README.md. From there inspect only the project documents and code needed to plan this task: {{TASK}}

Then read {{PLAN_TEMPLATE}} and reproduce its structure exactly, filled in — do not invent a different structure. Today's date is {{DATE}}.

Fill 'Delivers' with the IDs of the user stories this plan implements, read from docs/02-requirements.md or docs/02-user-stories.md. Cite the IDs only — never copy their acceptance criteria into the plan; the requirements document is the source of truth and the plan is deleted at closeout. If the task is refactoring, infrastructure or maintenance with no matching story, write 'None – <reason>' instead of inventing one.

Fill 'Gate profile' with the project's declared default from docs/00-ai-context.md's 'AI workflow' section ('Default gate profile') if it states one; otherwise use 'semi-automatic'. Only propose a different profile than the declared default when this specific task's risk clearly warrants it, and say why in the plan.

Size the plan as one mergeable increment: the smallest change that can merge on its own without leaving the product broken or half-migrated, usually a single user story. If the task as stated spans more than that — a milestone, several independent stories, or a sequence of increments — plan only the first increment, and list the rest under 'Excluded' with a note that they need their own plans. Do not compress a larger scope by making steps vaguer.

Scope: 'Included' and 'Excluded' must be concrete behaviors or surfaces, not themes; never skip 'Excluded'. Steps need real file paths and one runnable Verification command each. Classify ambiguity by consequence. Resolve ordinary implementation ambiguity from repository
evidence, documented project intent, established conventions, and the least-consequential reversible
choice. Record those choices as assumptions. Label unresolved material choices as **ESCALATION** or
**RESERVED**: reasonable alternatives that materially change scope, externally visible behavior,
architecture, security/privacy, cost, reversibility, or another project-reserved boundary. Definition of Done must be checkable conditions, not judgment calls — prefer "command X exits 0" over "works well".

Keep the whole plan under 200 lines. It is a work order for an agent that was not part of the planning and has no tacit context, so be more explicit than you would be writing for yourself.

In "Context to read first", cite the smallest stable project context that another agent needs. Use a backticked reference in one of two forms: `path/to/file.md` for a genuinely small whole file, or `path/to/file.md#heading selector` for one Markdown section. Prefer IDs in headings such as `docs/02-requirements.md#FR-017` or `docs/03-sad.md#ADR-012`. Never cite AGENTS.md/CLAUDE.md (the runtime already injects them), never cite `docs/archive/`, and never cite a whole permanent document merely for convenience. Whole-file references over 24 KiB are rejected by the context-pack builder; the complete pack is capped separately at 64 KiB.

Build nothing. You have no file-write access in this sandbox — do not attempt to create, edit, save, or patch any file, including the plan itself. Reply with the complete, finished plan as your final chat message text; the orchestrator captures that reply and writes it to file for you.

Before citing any file path in a step, Context to read first, or elsewhere in the plan, verify it exists (ls the directory or grep for it) rather than assuming it from a pattern or a similar path elsewhere in the codebase. A cited path that does not exist forces the build agent to guess what was meant, which is exactly the ambiguity this plan exists to remove.

If you find a 'plan-driven-build' or similar orchestration skill file in this repo (for example under .agents/skills/ or .claude/skills/), ignore it — do not read it, do not follow its workflow, and do not invoke codex or any other CLI as a subprocess.
